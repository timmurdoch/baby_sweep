version: '3.8'

services:
  baby-sweep:
    build: .
    container_name: baby-sweep
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
    volumes:
      - ./data:/app/data
    restart: unless-stopped
